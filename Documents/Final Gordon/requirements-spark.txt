-r requirements.txt

# cuDF wheels are CUDA-version specific. Install the matching RAPIDS wheel on GX10.
# Example for CUDA 12:
#   pip install --extra-index-url=https://pypi.nvidia.com cudf-cu12

# GPU ASR decision:
# Prefer the local ARM64 Parakeet TDT service in asr/parakeet-spark when it is
# running on port 8010. Fall back to NeMo / Parakeet on PyTorch CUDA when the
# GX10 app venv has a compatible ARM64 + GB10 PyTorch stack. The current
# faster-whisper path remains a CPU fallback because the available CTranslate2
# wheel reports no CUDA support.
#
# Build/run native DGX Spark ASR service:
#   cd asr/parakeet-spark
#   export NGC_API_KEY=...
#   ./build-and-run.sh
#
# Compatibility spike commands:
#   python - <<'PY'
#   import torch
#   print(torch.__version__, torch.cuda.is_available(), torch.version.cuda)
#   if torch.cuda.is_available():
#       print(torch.cuda.get_device_name(0), torch.cuda.get_device_capability(0))
#   PY
#
# If torch CUDA is available:
#   pip install 'nemo-toolkit[asr,cu13]'
